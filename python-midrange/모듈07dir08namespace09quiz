그냥 생각하는거 적어봅시다
import a

dir(a)

a파일에서 정의된 모든 함수들 이름만 불러옴 

dir로 불러오는것들중에 덥더로 씌워진 이름들이 있는데 아직 설명 안들음
덥더 더블언더스코어



namespace 듣고옵시다
아하 이름들이 저장되는 공간이네
나중에 정의된 이름이 저장됨
import a   (a 에 square 함수가 있음)
def square(a)
  return a*a
  
square(3)
이러면 나중에 정의된 a*a가 불러와짐

이래서 form a import * 가 안쓰임 이름이 중복될수도 있어서

a.square 이렇게 쓰면되는데
import * 쓰면 나중에 정의된 square랑 이름이 겹쳐서 안써짐

