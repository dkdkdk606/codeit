import math
import sys

sys.path.append('c:||dkdkd||한글도되려나||codeit||Destktop')

print(sys.path)
#파일경로 하는거 위꺼 한글도 되는지 확인하고싶은데 힘드네 random datetime math copy정도만 기억하면 될듯 나머지는 뭔소리인지 모르겠음
#파이썬이 모듈을 찾는 경로 확인하고 경로 추가하고 영구적으로 추가하고
#모듈이랑 스크립트
#모듈 import 하면 함수 불러오는것처럼 모든것 다가져옴 모듈안에 print 있으면 import 하는순간 print도 같이 됨 함수만 따로 가져오는 기능을 그래서 만들어놓은듯
# 모듈의 이름을 -*자동*-으로 저장해놓은 변수 __name__ 지금 현 파일은 __main__ 나머지는 파일 이름
# if __name__ == '__main__':
#   main()
#자바같은데서는 main만 실행되는데 그것 비슷하게 여기로 불러와서 실행문 main안에 다 써놓으면 실행문+함수가 있어도 모듈로 쓸 경우에는
#함수만 꺼내 사용할 수 있음 장점이 된다
